# PostgreSQL 설정
# ⚠️ 이 파일을 복사하여 .env 파일을 만들고 실제 값으로 수정하세요!
# 명령: cp .env.example .env (Linux/Mac) 또는 copy .env.example .env (Windows)

POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=ai_innovation_db
POSTGRES_PORT=5432

# pgAdmin 설정
PGADMIN_DEFAULT_EMAIL=your_email@example.com
PGADMIN_DEFAULT_PASSWORD=CHANGE_THIS_PASSWORD
PGADMIN_PORT=5050

# ⚠️ 보안 주의사항:
# 1. POSTGRES_PASSWORD와 PGADMIN_DEFAULT_PASSWORD를 반드시 변경하세요!
# 2. 강력한 비밀번호를 사용하세요 (최소 12자, 대소문자+숫자+특수문자)
# 3. .env 파일은 절대 Git에 커밋하지 마세요! (.gitignore에 포함됨)
# 4. 프로덕션 환경에서는 더욱 엄격한 보안 정책을 적용하세요.
